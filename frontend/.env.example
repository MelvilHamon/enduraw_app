# Base URL for the API. Leave empty in dev (the Vite proxy forwards /api to the
# FastAPI backend) and in prod (FastAPI serves this build on the same origin).
# Set it only when the frontend calls a remote API directly, e.g.
# VITE_API_BASE_URL=https://api.example.com
VITE_API_BASE_URL=
